<?php
$custom = isset($ativo['customAttributes']) ? $ativo['customAttributes'] : null;

if (is_array($custom) && count($custom)) {
    foreach ($custom as $key => $value) {
        $label = isset($atributos[$key]) ? $atributos[$key] : $key;
        echo '<h6>' . $label . '</h6><h5>' . $value . '</h5>';
    }
} else {
    echo '<h5>sem dados</h5>';
}
?>
